apiVersion: v1
kind: Pod
metadata:
  name: static-web
  labels:
    role: myrole
spec:
  containers:
    - name: web
      image: neelakantareddivari/ow2:V1
      ports:
        - name: web
          containerPort: 80
          protocol: TCP
